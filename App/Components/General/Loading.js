import React, {
	Component,
} from 'react';
import{
  Modal,
  View,
  Text,
  TouchableHighlight,
} from 'react-native';

// import { ADLoadingBridge} from 'NativeModules';
let loading = false;
export default (props) =>{
    // if(props.loadingVisible && !loading){
    //   loading = true;
    //   ADLoadingBridge.showProgressLoaderWithSuccess();
    // }else if(!props.loadingVisible && loading){
    //   loading = false;
    //   ADLoadingBridge.showProgressResult(props.loadingResult);
    // }
		// if(props.loadingVisible){
		// 	return (
		// 			<View style={{position:'absolute',top:0,left:0,bottom:0,right:0}}>
		// 			</View>
	  //   );
		// }else{
		// 	return null
		// }
}
